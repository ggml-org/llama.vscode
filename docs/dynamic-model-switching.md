# Dynamic model switching with llama-swap

This document demonstrates how to dynamically switch between
multiple LLaMA models hosted on a remote llama-swap server
while using the llama.vscode extension.

The example uses a Python-based model switcher that selects
models based on task type and latency requirements.
